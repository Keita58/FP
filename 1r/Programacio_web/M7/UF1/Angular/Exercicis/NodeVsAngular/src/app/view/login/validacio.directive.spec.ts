import { ValidacioDirective } from './validacio.directive';

describe('ValidacioDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacioDirective();
    expect(directive).toBeTruthy();
  });
});
